{
  "name": "smart-indexer-server",
  "version": "0.0.4",
  "description": "Language server for Smart Indexer",
  "main": "./out/server.js",
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -p . --watch",
    "test:worker": "tsc -p . && node out/test/workerTest.js"
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.1",
    "vscode-languageserver-textdocument": "^1.0.12",
    "vscode-uri": "^3.1.0"
  },
  "devDependencies": {
    "@types/node": "22.x",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
